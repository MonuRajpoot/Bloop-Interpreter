public enum TokenType {
    // Keywords for bloop language
    PUT, INTO, PRINT, IF, THEN, REPEAT, TIMES,

    // Data types for language(bloop)
    NUMBER, STRING, IDENTIFIER,

    // Operators for languange(bloop)
    PLUS, MINUS, MULTIPLY, DIVIDE,
    GREATER, LESS, EQUAL,

    // Symbols for bloop language
    LPAREN, RPAREN,

    // special Symbol
    EOF
}